services:
  petPal-app:
    image: semester3-individualproject-petpal:latest
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "8090:8080"

    environment:
      JAVA_OPTS: ""

    restart: unless-stopped